# CLAUDE.md

Claude Code 세션용 개발 규칙 요약. 운영 서비스 저장소이며 `main` 루트가 GitHub Pages로 공개 배포된다.
제품 구조·보안 원칙의 원문은 `AGENTS.md`를 따른다. 이 문서는 실행 규칙만 담는다.

## 작업 흐름

- 최신 `main`에서 작업 브랜치를 만들어 작업한다. 기존 브랜치·열린 PR이 있으면 그것을 이어서 쓴다.
- 작업 브랜치 → 커밋 → PR 순서로 진행한다. `main`에 직접 push하지 않는다.
- merge는 사용자 승인 후에만 한다.
- `main` 병합은 곧 공개 배포다. 병합 전 상태를 배포 가능한 상태로 유지한다.

## 완료 기준

수정 → 관련 테스트와 CI 통과 → PR 생성 → 결과 보고. CI 결과를 확인하기 전에는 완료로 보고하지 않는다.

## 정지 지점

아래 작업은 적용 직전에 멈추고, 적용 순서와 복구(rollback) 경로를 보고한 뒤 승인을 받는다.

- DB migration 적용
- Supabase 권한·RLS 변경
- Edge Function 배포
- Cloudflare 설정 변경
- production 데이터 변경

## 보안

- 인증 우회, RLS 약화, 권한 확대를 문제 해결 수단으로 쓰지 않는다.
- 권한 판단은 서버·DB 계층에서 한다. 클라이언트가 보낸 소유자·역할 값만 믿지 않는다.
- 공개 저장소다. 키, 토큰, 내부 전용 주소, 개인정보를 코드·문서·커밋에 넣지 않는다.
- 디버그용 권한 예외나 임시 우회 코드를 남기지 않는다.

## 범위

- 요청 범위만 수정한다. 범위 밖에서 발견한 문제는 고치지 않고 보고만 한다.
- 관련 없는 구조 변경, 기능 추가, 재설계를 하지 않는다.

## 완료보고 형식

1. 원인
2. 변경 내용
3. 검증·CI 결과
4. 남은 위험
5. 브랜치·PR·배포 상태

## 참고 문서

필요한 작업에 해당할 때 찾아본다.

- `app/ARCHITECTURE.md`: Web2 앱 진입 경로, 모듈 로딩, 화면 전환 소유권 구조.
- `docs/bokdoong-domain.md`: bokdoong.com 도메인 연결과 호스트·경로 운영 설계.
- `docs/web2-expand-deploy-verify-contract.md`: Web2 공개 경로 변경의 Expand → Deploy → Verify → Contract 배포 순서.
