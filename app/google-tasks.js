(()=>{
'use strict';
if(window.__KPTU_GOOGLE_TASKS__)return;
window.__KPTU_GOOGLE_TASKS__=true;
const rt=window.KPTURuntime;let loading=false,lastTasks=[];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const endpoint=action=>`/functions/v1/google-tasks?action=${encodeURIComponent(action)}`;
const due=v=>{if(!v)return'기한 미정';const raw=String(v).slice(0,10),d=new Date(raw+'T00:00:00');return Number.isNaN(d.getTime())?'기한 미정':d.toLocaleDateString('ko-KR',{month:'numeric',day:'numeric',weekday:'short'})};
function section(){let sec=document.querySelector('#gtTaskSection');if(sec)return sec;const taskList=document.querySelector('#taskList');if(!taskList)return null;sec=document.createElement('section');sec.id='gtTaskSection';sec.className='tl-task-section';sec.innerHTML='<div class="gt-head"><div class="gt-head-left"><h3 style="margin:0;font-size:17px">Google 할 일</h3><span class="gt-source">G</span><span class="gt-note">Workspace 할 일과 별도 · 읽기 전용</span></div><button class="mini gt-refresh" type="button" data-gt-refresh>새로고침</button></div><div id="gtTaskBody" class="gt-status">불러오는 중…</div>';taskList.insertAdjacentElement('afterend',sec);sec.querySelector('[data-gt-refresh]').onclick=()=>load(true);return sec}
function renderTasks(tasks){const body=section()?.querySelector('#gtTaskBody');if(!body)return;if(!tasks.length){body.className='gt-status';body.innerHTML='미완료 Google 할 일이 없습니다.';return}body.className='gt-list';body.innerHTML=tasks.map(t=>`<div class="gt-row" data-google-task="${esc(t.id)}"><span class="gt-mark" aria-hidden="true"></span><div class="gt-content"><b>${esc(t.title||'제목 없음')}</b><div class="gt-meta"><span>${esc(t.taskListTitle||'Google Tasks')}</span><span>${esc(due(t.due))}</span>${t.notes?`<span>${esc(t.notes)}</span>`:''}</div></div></div>`).join('')}
function renderConnect(message='Google 할 일을 함께 보려면 Google 계정 권한을 연결해 주세요.'){const body=section()?.querySelector('#gtTaskBody');if(!body)return;body.className='gt-status';body.innerHTML=`<div>${esc(message)}</div><button class="secondary gt-connect gt-reconnect" type="button" data-gt-connect>Google 할 일 연결</button>`;body.querySelector('[data-gt-connect]').onclick=connect}
function renderError(message){const body=section()?.querySelector('#gtTaskBody');if(body){body.className='gt-status';body.innerHTML=`<span style="color:#a33b45">${esc(message)}</span>`}}
async function connect(){try{if(!rt?.session||!(await rt.session.ensure()))return;const platform=window.__KPTU_NATIVE_BRIDGE__?'android':'web',d=await rt.api(endpoint('start')+'&platform='+platform);if(d?.url)location.href=d.url}catch(e){renderError(e.message||String(e))}}
async function load(force=false){if(loading)return;const sec=section();if(!sec)return;if(!force&&lastTasks.length){renderTasks(lastTasks);return}if(!rt?.session||!(await rt.session.ensure()))return;loading=true;const body=sec.querySelector('#gtTaskBody');if(body){body.className='gt-status';body.textContent='Google 할 일을 불러오는 중…'}try{const d=await rt.api(endpoint('tasks'));if(d?.needs_reconnect){lastTasks=[];renderConnect(d.warning||'Google 할 일을 보려면 Google 계정을 한 번 다시 연결해 주세요.');return}if(d?.error)throw new Error(d.error);lastTasks=d?.tasks||[];renderTasks(lastTasks)}catch(e){const msg=String(e?.message||e);if(/연결되지|재인증|TASKS_SCOPE_REQUIRED/i.test(msg))renderConnect();else renderError(msg)}finally{loading=false}}
async function boot(){section();window.KPTURouter?.on?.('tasks',()=>load(true));window.addEventListener('focus',()=>{if(!document.querySelector('#tasksView')?.classList.contains('hidden'))load(true)});window.addEventListener('kptu:session-changed',()=>{lastTasks=[];load(true)});await load(false)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>boot().catch(console.error),{once:true});else boot().catch(console.error);
})();
