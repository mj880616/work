# Legacy app modules

이 폴더는 현재 `app/app.js` → `loader-v2.js` 런타임 진입 경로에서 로드되지 않는 과거 보정 모듈을 격리한 곳임.

- 런타임 의존성 검사에서 비활성으로 확인된 파일만 이동함.
- 새 기능에서 이 파일을 다시 import하지 않음.
- 필요한 과거 구현을 참고할 때만 열람하고, 재사용이 필요하면 현재 소유 모듈에 기능을 통합함.
