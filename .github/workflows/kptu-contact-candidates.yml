name: Build KPTU contact author candidates
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/kptu_contact_candidates.py'
      - '.github/workflows/kptu-contact-candidates.yml'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/kptu_contact_candidates.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis/kptu-corpus/contact-author-candidates.json
          git diff --cached --quiet || git commit -m "Build KPTU contact author candidates"
          git pull --rebase origin main
          git push
