name: Map selected KPTU contact authors
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/kptu_contact_selected.py'
      - '.github/workflows/kptu-contact-selected.yml'
permissions:
  contents: write
jobs:
  map:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install requests beautifulsoup4
      - run: python scripts/kptu_contact_selected.py
      - name: Commit sample map
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis/kptu-corpus/contact-authors-selected-180.json
          git diff --cached --quiet || git commit -m "Map authors in selected KPTU sample"
          git pull --rebase origin main
          git push
